services:
  bot:
    build: .
    container_name: telegram_moderation_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    # Если используете удаленную БД (Supabase), закомментируйте строку ниже
    # depends_on:
    #   db:
    #     condition: service_healthy
    networks:
      - bot_network

  # Локальная БД для разработки (закомментировано, используется Supabase)
  # Раскомментируйте для использования локальной БД:
  # db:
  #   image: postgres:15-alpine
  #   container_name: telegram_moderation_db
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_DB: ${DB_NAME:-moderation_db}
  #     POSTGRES_USER: ${DB_USER:-postgres}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
  #   ports:
  #     - "5432:5432"
  #   networks:
  #     - bot_network
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

# Раскомментируйте для использования локальной БД:
# volumes:
#   postgres_data:

networks:
  bot_network:
    driver: bridge

